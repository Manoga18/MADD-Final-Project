<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.maddfinalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MADDFinalProject">
        <activity android:name=".Calldesigner"></activity>
        <activity android:name=".Portfolio" />
        <activity android:name=".DesignerMainUI" />
        <activity android:name=".Rentstore" />
        <activity android:name=".RentProductView" />
        <activity android:name=".Menu" />
        <activity android:name=".callui" />
        <activity android:name=".works"
            tools:ignore="Instantiatable" />
        <activity android:name=".Rents" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>